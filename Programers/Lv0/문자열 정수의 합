using System;
 
public class Solution 
{
    public int solution(string num_str) 
    {
        int sum = 0;
        
        for (int i = 0; i < num_str.Length; i++)
        {
            sum += int.Parse(num_str[i].ToString());
        }
        
        return sum;
    }
}
