using System;
 
public class Solution
{
    public string solution(string myString, string pat)
    {
        string r = "";
        for (int i = 0; i < myString.LastIndexOf(pat); i++)
        {
            r += myString[i].ToString();
        }
        return r + pat;
    }
}
