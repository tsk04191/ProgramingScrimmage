using System;
 
public class Solution
{
    public int solution(int n)
    {
        int sum = 0;
        string s = n.ToString();
        
        for (int i = 0; i < s.Length; i++)
        {
            sum += int.Parse(s[i].ToString());
        }
        
        return sum;
    }
}
