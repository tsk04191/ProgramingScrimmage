using System;
 
class Solution
{
    public long solution(int price, int money, int count)
    {
        long t = (long)price * count * (count + 1) / 2;
        long r = t - money;
        
        return r > 0 ? r : 0;
    }
}
