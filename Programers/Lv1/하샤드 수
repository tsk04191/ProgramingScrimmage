public class Solution
{
    public bool solution(int x)
    {
        int target = x, sum = 0;
        
        while(x > 0)
        {
            sum += x % 10;
            x /= 10;
        }
        
        return target % sum == 0;
    }
}
